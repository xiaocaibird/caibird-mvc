/**
 * @Creater cmZhou
 * @Desc async helper
 */
import { HAsync as base } from '../../@common/helper/hAsync';

export abstract class HAsync<TCustomRunOpt extends object = {}> extends base<TCustomRunOpt> {

}
