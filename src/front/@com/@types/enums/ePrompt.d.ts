/**
 * @Owners cmZhou
 * @Title prompt 常用枚举
 */
declare namespace ePrompt {
    namespace F {
        const enum Type {
            Modal = 0,
            Toast = 1,
        }
        const enum StyleType {
            Warning = 0,
            Info = 1,
            Error = 2,
            Success = 3,
            Confirm = 4,
        }
    }
}
